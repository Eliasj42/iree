name: Windows Runner Test

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test-windows-runner:
    runs-on: azure-windows-scale

    steps:
    - name: Hello World
      run: echo "Hello World!"